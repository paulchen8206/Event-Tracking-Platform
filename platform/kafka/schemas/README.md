# Kafka Schemas

Avro schemas under `avro/` are the transport schemas registered with Schema Registry for Kafka value serialization.

These are transport-oriented schemas. They complement, but do not replace, the broader canonical event contracts under `shared/contracts/events/`.
