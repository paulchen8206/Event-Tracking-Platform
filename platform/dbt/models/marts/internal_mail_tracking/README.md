# Internal Mail Tracking Marts

Use this folder for models that support internal workflow visibility, support investigations, and operational monitoring.

Typical contents:

- latest message state views
- retry and failure tracking models
- provider latency and SLA models
- queue backlog and exception monitoring marts

These models can retain more operational detail than customer-facing marts, including diagnostic attributes and internal-only states.
