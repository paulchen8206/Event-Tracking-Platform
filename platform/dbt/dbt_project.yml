name: event_tracking_platform
version: 1.0.0
config-version: 2
profile: event_tracking_platform
model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]
target-path: target
clean-targets:
- target
- dbt_packages
models:
  event_tracking_platform:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table